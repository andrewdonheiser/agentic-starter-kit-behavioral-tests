# Benchmark: agentic-latency
# Tests response latency against configurable thresholds.
# max_latency_seconds is set via config.parameters (default 10s).

queries:
  - query: "What is Red Hat OpenShift?"
    expected_tools: ["search"]
    expected_elements: ["openshift"]
    difficulty: easy
    category: factual

  - query: "Hello"
    expected_tools: []
    expected_elements: []
    difficulty: easy
    category: greeting

  - query: "Compare Kubernetes and OpenShift for enterprise ML workloads"
    expected_tools: ["search"]
    expected_elements: ["kubernetes", "openshift"]
    difficulty: medium
    category: multi_part

  - query: "Is it better to use vLLM or TGI for serving large language models in production and what trade-offs should I consider?"
    expected_tools: ["search"]
    expected_elements: ["model"]
    difficulty: hard
    category: ambiguous
